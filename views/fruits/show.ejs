<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= fruit.name %></title>
</head>
<body>
  <h1><%= fruit.name %></h1>


  <% if(fruit.isReadyToEat){ %>
    <p>It is safe to eat this fruit!!!</p>
  <% }else{ %>
    <p>Better wait a little longer!</p>
  <% } %>

  <form action="/fruits/<%=fruit._id%>?_method=DELETE" method="POST">
    <button type="submit">Delete <%= fruit.name %></button>
  </form>
  <a href="/fruits/<%= fruit._id %>/edit">Edit <%= fruit.name %></a>
  <a href="/fruits">Go back to all fruits</a>
</body>
</html>